apiVersion: v1
kind: Service
metadata:
  name: kapt-api-service
  namespace: default
  labels:
    app: kapt-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: kapt-api
